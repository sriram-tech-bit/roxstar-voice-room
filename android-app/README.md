# Android app

Kotlin UI for the Roxstar assessment. Native code lives in `/native-audio` and is built with CMake + Oboe.

Open this folder (`android-app`) in Android Studio. First native build downloads Oboe.

`minSdk 24`. Permission: `RECORD_AUDIO`.
