# Tests

- Unit tests for the spin state machine: `backend/tests/spinEngine.test.js`
- Health contract: `backend/tests/health.test.js`

```bash
cd backend && npm test
```

Integration against a live API (room + spin) is documented in `spin-room.integration.md`.
